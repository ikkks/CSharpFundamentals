//Declare an integer variable and assign it with the value 254 in hexadecimal format. 
//Use Windows Calculator to find its hexadecimal representation.

using System;

namespace HexValue
{
    class HexValue
    {
        static void Main()
        {
            //Declare variable
            int hexValue = 0xFE;
            Console.WriteLine("FE is the Hex Value of {0}", hexValue);
        }
    }
}
